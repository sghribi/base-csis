{% extends 'CSISEamBundle::layout.html.twig' %}

{% block csis_body %}
    <div class="row">
        <div class="span12">
            {% include 'CSISEamBundle:Eam:flash.html.twig' %}
            {% include 'CSISEamBundle:Eam:mainflash.html.twig' %}
        </div>
    </div>

    <div class="row">
        {% include 'CSISEamBundle:Eam:flash.html.twig' %}
        <form action="{{ path('laboratory_create') }}" method="post" {{ form_enctype(form) }} class="well offset1 span10">
            <fieldset>
                <legend>
                  Céation d'un laboratoire
                  <a href="{{ path('laboratory') }}" class="btn btn-default pull-right" data-toggle="tooltip" title="Retour à la liste des laboratoires">
                    <i class="icon-list-alt icon-white"></i>
                    Liste des laboratoires
                  </a>
                </legend>
                {{ form_widget(form) }}
                {{ form_rest(form) }}
                <div class="pull-right">
                    <button type="submit" class="btn btn-success">
                        <i class="icon-share-alt icon-white"></i>
                        Créer le laboratoire
                    </button>
                    <a href="{{ url('csis_eam_admin_index') }}" class="btn btn-default">Annuler</a>
                </div>
            </fieldset>
        </form>
    </div>
{% endblock %}
